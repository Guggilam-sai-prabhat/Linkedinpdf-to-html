<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Upload LinkedIn PDF Resume</title>
<style>
    @keyframes gradient {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }
    body {
        font-family: 'Arial', sans-serif;
        background: linear-gradient(-45deg, #0077b5, #00a0dc, #8ccbf1, #b8e5f8);
        background-size: 400% 400%;
        animation: gradient 15s ease infinite;
        color: #2a2a2a;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        overflow: hidden;
    }
    .container {
        background: rgba(255, 255, 255, 0.89);
        backdrop-filter: blur(50%);
        padding: 30px 40px;
        border-radius: 8px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        text-align: center;
        width: 350px;
    }
    h1 {
        color: #2867b2;
        font-size: 24px;
        margin-bottom: 20px;
    }
    form {
        margin-top: 10px;
    }
    input[type="file"], input[type="text"] {
        padding: 8px;
        background: transparent;
        border: 2px solid #ccc;
        border-radius: 4px;
        width: 100%;
        box-sizing: border-box;
        font-size: 14px;
        margin-bottom: 15px; /* Adds space between the inputs */
    }
    button {
        background-color: #0073b1;
        border: none;
        color: white;
        padding: 10px 16px;
        text-align: center;
        text-decoration: none;
        display: block;
        font-size: 16px;
        margin-top: 20px;
        cursor: pointer;
        border-radius: 4px;
        width: 100%;
        transition: background-color 0.3s ease;
    }
    button:hover {
        background-color: #005580;
    }
</style>

</head>
<body>
    <div class="container">
        <h1>Upload Your LinkedIn PDF Resume</h1>
        <form action="/upload" method="post" enctype="multipart/form-data">
            <input type="file" name="file" required>
            <input type="text" name="marker" placeholder="Enter LinkedIn Name" required> <!-- New input for dynamic marker -->
            <button type="submit">Convert to HTML</button>
        </form>
    </div>
</body>
</html>
